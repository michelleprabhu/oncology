streamlit>=1.0.0
pandas>=1.3.0
langchain>=0.0.150  # A stable version that avoids 'langchain_community' issues
langchain-openai>=0.1.0  # For OpenAI integration with Langchain (if you're using OpenAI models specifically)
openai>=0.27.0  # For the OpenAI API
pydantic>=2.0.0  # For handling data validation
openpyxl>=3.1.0  # For reading Excel (.xlsx) files
xlrd>=2.0.1  # For handling older Excel files (.xls)
aiohttp>=3.8.0  # For making asynchronous HTTP requests (used in your code)
